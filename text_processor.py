"""
Provides text processing functions such as cleaning, tokenizing, and counting words.

Functions:
    clean_text(text): Cleans the input text by removing punctuation and converting to lowercase.
    tokenize(text): Splits the cleaned text into tokens or words.
    count_words(text): Counts the occurrences of each word in the text.
"""
